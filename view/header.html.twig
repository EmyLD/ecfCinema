<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script
      src="https://kit.fontawesome.com/99d22a9197.js"
      crossorigin="anonymous"
    ></script>
  </head>
  <body>
    <header>
      <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <!-- Brand/logo -->
        <div class="">
          <a href="home" class="navbar-brand"
            ><i class="fa-solid fa-film"></i
          ></a>
          <a class="navbar-brand" href="home">Médiatèque</a>
        </div>

        <!-- Links -->
        <div class="d-flex flex-row justify-content-between col align-items-center">
          <ul class="navbar-nav">
            <li class="nav-item">
              <a class="nav-link" href="home">Accueil</a>
            </li>
			{% if session.user is defined %} Bonjour
            {{ session.user }}
			<li class="nav-item">
				<a class="nav-link" href="creer">Création</a>
			</li>
			<button class="btn btn-outline-light text-light " type="button" >Logout</button>
            {% else %}
			<li class="nav-item">
			  <a class="nav-link" href="login">Compte</a>
			</li>
			 L'utilisateur n'est pas connecté 
			 {% endif %}
          </ul>

          <form class="form-inline my-2 my-lg-0">
            <div class="me-2">
              <input type="text"  placeholder="Rechercher un film"/>
              <button class="btn btn-outline-light text-light " type="button" >Rechercher</button>
            </div>
          </form>
        </div>
      </nav>
      
          <div class="col">
            {% if session.user is defined %} Bonjour
            {{ session.user }}
            {% else %} L'utilisateur n'est pas connecté {% endif %}
          </div>
        </div>
      </div>
    </header>
    <hr />
  </body>
</html>
